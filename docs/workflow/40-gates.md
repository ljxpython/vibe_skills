# 强制门禁（每次改动都必须）

任何会产生改动的任务，在“结束前”必须完成以下三项，否则一律 BLOCK。

## 1) 审计门禁

- 命令：`/审计`
- 产物：`docs/review.md`
- 结果：PASS/BLOCK（必须给出阻断理由）

## 2) 测试计划门禁

- 命令：`/测试计划`
- 产物：`docs/test-plan.md`
- 覆盖：本次改动 + E2E + 核心逻辑 + 每接口

## 3) 执行测试门禁

- 命令：`/执行测试`
- 产物：`progress.md`（执行证据：命令 + 关键输出摘要）
