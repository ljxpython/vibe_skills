---
description: "问题复盘：按 problem-finder 规范总结问题并写入 docs/problem.md（写前先复用历史）。"
agent: build
---

步骤：
1) 确保 `docs/problem.md` 存在。
2) 写入前先检索 `docs/problem.md` 是否已有相似问题（至少 2 组关键词）。
3) 生成并追加一条问题记录，必须包含：
   - 问题描述、影响范围、根因、解决方案、关键修改、验证方式、参考链接
