---
description: "审计门禁：按 code-audit 规范输出检查清单并写入 docs/review.md（PASS/BLOCK）。"
agent: build
---

执行审计门禁（每次改动都必须）。

路由提示（强制）：
- 生成结论由 @gate-audit 负责
- 落盘由 @scribe 负责写入 docs/review.md

步骤：
1) 确保 `docs/` 存在；确保 `docs/review.md` 存在。
2) 读取审查规范：`.opencode/skills/code-audit/references/audit-prompt.md`
3) 委托 @gate-audit 生成“可审计检查清单 + 逐项验证 + PASS/BLOCK”。
4) 委托 @scribe 将结果追加写入 `docs/review.md`（带日期标题）。
5) 输出最终结论（PASS/BLOCK）与阻断理由摘要。
