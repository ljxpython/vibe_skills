---
description: "写入并启用本仓库的启动治理协议：强制门禁、默认并行、落盘规则、opencode-only 约束。"
agent: plan
subtask: true
---

执行目标：把“启动规则”固化为权威文档，并确保当前项目符合 opencode-only。

步骤：
1) 确认当前项目为 opencode-only：
   - `.opencode/` 存在
   - 禁止引用 `.claude/` / `agent_config/`
2) 加载 skill：`kickoff-rules`
3) 生成/更新 `docs/workflow/40-gates.md` 与 `docs/workflow/00-overview.md` 的关键内容（由文档书记写入）：
   - 每次改动强制门禁（审计/测试计划/执行测试）
   - 默认并行检索策略（代码侦察 + 资料检索 + MCP augment）
   - 落盘策略（findings/progress/docs）
4) 输出“当前项目已启用启动规则”的简短确认，并提示下一步使用：
   - `/改动开始`
   - `/最大化检索 <query>`
