---
description: "工作流帮助：列出可用命令/子代理/MCP/skills，并给出路由范例。"
agent: build
---

请输出本仓库 opencode-only 工作流的“可用能力清单 + 路由范例”。

输出结构必须固定为：

1) Commands
- 列出 `.opencode/commands/` 下主要命令（只列名字 + 一句话用途）

2) Subagents
- 列出可用子代理（@code-scout/@librarian/@workflow-designer/@gate-audit/@gate-test/@scribe）与各自职责

3) Skills
- 列出关键 skills（env-init/kickoff-rules/resume-handshake/planning-with-files/code-audit/test-coverage/problem-finder/ui-ux-pro-max）与适用场景

4) MCP
- 列出：augment-context-engine

5) 路由范例（至少 3 个）
- 首次初始化：/keep-off
- 不确定检索：/最大化检索
- 产生改动：/改动开始 -> /审计 -> /测试计划 -> /执行测试

要求：
- 只使用本仓库已有的命令与智能体名称，不要虚构。
