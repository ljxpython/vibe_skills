---
description: "最大化检索：并行代码侦察+资料检索+MCP(augment)，输出证据清单与结论。"
agent: build
---

目标：最大化检索，不停在第一个结果。

路由提示（强制）：
- 必须并行：@code-scout（仓库内）+ @librarian（外部资料）
- 必须使用：MCP augment-context-engine（如可用）
- 输出结构必须是：Evidence / Conclusions / Unknowns

输入：`$ARGUMENTS`

要求：
1) 并行委托：
   - @code-scout：在本仓库内找与查询相关的实现/配置/文档证据
   - @librarian：在外部资料中找官方文档与真实示例（必须附 URL）
2) 同时使用 MCP augment-context-engine（如可用）补充代码库语义检索。
3) 最终输出三段：
   - Evidence：逐条列出（路径/URL + 摘要）
   - Conclusions：可执行结论
   - Unknowns：仍需确认的问题
