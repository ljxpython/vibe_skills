---
description: "改动开始：定义成功标准（功能/可观测/二元通过），并声明本次改动必须门禁。"
agent: plan
---

你正在开始一次“会产生改动”的任务，必须先定义成功标准。

路由提示（强制）：
- 如果目标/范围不清：先用 `/最大化检索` 并行补证据再改
- 本次任务结束前必须运行：`/审计` + `/测试计划` + `/执行测试`

步骤：
1) 让用户用 1-3 句话描述目标（或用 $ARGUMENTS）。
2) 输出成功标准表：
   - Functional
   - Observable
   - Pass/Fail
3) 明确宣告强制门禁：本次结束前必须运行 `/审计` + `/测试计划` + `/执行测试`。
4) 让用户确认成功标准后再进入实现。
