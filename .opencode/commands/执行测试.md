---
description: "执行测试：运行项目测试/构建命令并把证据写入 progress.md（每次改动必做）。"
agent: build
---

执行测试并记录证据（每次改动都必须）。

步骤：
1) 让用户确认本项目的测试命令（若未提供，先扫描 README/package.json/pyproject 再推断；不确定就问）。
2) 运行测试/构建命令（按项目实际情况）。
3) 将证据写入 `progress.md`：
   - 执行时间
   - 运行的命令
   - 关键输出摘要（通过/失败点）
   - 若失败：失败原因与下一步
